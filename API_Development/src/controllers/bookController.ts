import express from "express";

const getAllBooksController: () => Promise<T> = async() => {

};

const getBookController: () => Promise<T> = async () => {};
export default {
  getAllBooksController,
  getBookController,
};